

## Plan: Update index.html title

Change the `<title>` tag and `og:title` meta tag in `index.html` from "Lovable App" to "VerbIA Agents".

### Files to modify
- **index.html**: Update `<title>` (line 7) and `og:title` meta (line 13) to "VerbIA Agents"

